# Define the list of dynamic crypto/stock symbols to track
SUPPORTED_SYMBOLS="BTCUSDT,XAUTUSDT,ETHUSDT,BNBUSDT"

# Port for the express server
PORT=3000

# Secret key for admin endpoints (/api/notice, /api/broadcast)
ADMIN_SECRET=changeme_use_a_strong_secret_here
